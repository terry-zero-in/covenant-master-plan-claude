# 00 — EXECUTIVE VERDICT AND CURRENT-STATE SCORE

**Package:** COVENANT-MASTER-PLAN-CLAUDE · produced by a Claude session against handoff zip SHA-256 `e87c3fcf7ab6417545b79d6e3b6df6fa3009628af73d028d2f1806746aa874b6` (identity record: deliverable 01 §0). Build target acknowledged: the Next.js application in the **basis-v2 repository** — the `(covenant)` routes and components within it, no prototype.

## 1. The verdict against Terry's framing

Terry's sentence — *"much more 'screens' to show and a plan in our head… as it relates to actually having a full product from A to Z it's just not there"* — is measurably correct, and precisely diagnosable:

**What Covenant has is a body without a nervous system.** The screens exist (29 component directories, a shell that survived adversarial benchmarking with zero REBUILDs). The organs exist (a tested, green engine library covering exactly the right domain chain: requirements → metrics → tests with watch bands → tie-out → send gate; an orchestration safety core with the ruled state machine and three human gates). What does not exist is the wiring between them — and, critically, **the four kinds of machinery no screen can fake**: the intake pipeline (gap 1), the document-processing chain (gap 2), the persisted data model with tenancy (gap 3), and the orchestration spine that runs DOCUMENT IN → RECORD as one system (gap 5). Every figure on every screen except one trend wire and one API route is a demo string.

**Current-state score: 3.5 / 10 as a product; 7.5 / 10 as raw material.** The distance is not quality — the built layers are unusually good — it is that the product's center of mass (the agent preparing a real package from real documents to a real gate) has no running instance. The honest state is exactly what the snapshot says: engine built, surfaces built, the two not yet connected.

## 2. What stands (do not rebuild)

1. **The engine spine** — pure, tested, fail-closed; its architecture matches the domain chain this plan mapped from the real evidence (deliverable 05). Wiring order, not rework.
2. **The shell chassis** — rail/header/canvas with the breadcrumb altitude gate; PASS with adjust riders (addressable deep routes, crumb switchers).
3. **The send vertical** — `POST /api/covenant/send` is the proven pattern (route carries loan+period, engine checks, persistence records, failures honest). The whole roadmap generalizes this seam.
4. **The design system** — ruled, reconciled, final (periwinkle accent family, ten-rung ladder, certified-sheet blue-violet). This plan consumes it and proposes zero color changes.
5. **The domain corpus and the demo canon** — five real-deal studies, the extraction checklist, the covenant census, canon-clean fixtures. This plan added a sixth evidence pillar: the full Calloway Park kit traced end to end (T-12 → package tie-outs verified by inspection; the 6001.NR agreement's §8.02(b) obligations extracted as the requirement-taxonomy ground truth; the roll's 322-rows/301-units dedup fact).
6. **The Reports list pattern** (ruled gold standard) and the reconcile room's three-column layout (ruled KEPT).

## 3. Systemic gaps (the four, plus three semantic corrections)

The four machinery gaps above, plus corrections the briefs make structural rather than editorial: (a) **your-move ≠ unread** — one computed query rendered everywhere (kills U3-F7 and the U3-F2 contradiction class); (b) **monitored ≠ covenanted** — the basis field and verdict-vocabulary ceiling enforced in types, so "breach" cannot be emitted for a monitored miss; (c) **borrower-first everywhere** — remaining lender-side framing (risk-ranked queues, surveillance posture) is retired in the briefs, and the sign-in page's cross-product link is a P0 one-line fix (WO-001).

## 4. Rebuild boundary

Exactly one REBUILD: **the outward door** (guest experience; the benchmark's one BLOCK). Everything else is ADJUST (18 units), PASS (7), or UNBUILT-build-to-brief (9). Matrix: deliverable 15.

## 5. Target end state (what "A to Z" means, concretely)

A borrower forwards a lender email to their Covenant address and walks away. The agent recognizes the loan, holds the period until the checklist completes, chases what is missing, extracts and proposes, the engine computes every number deterministically, the package assembles itself with drafted narratives, and the borrower's ~60 seconds are an exception walk where every figure lights its source line — then two deliberate ceremonies (certify: typed, identity-bound, hash-recorded; send: gate-verified approved bytes) and a sealed, forever-inspectable record that answers the lender's follow-ups with citations. Monthly, quarterly, annually, across 2–50 loans of every debt type, with the agent never certifying, never sending, never touching a shipping number — and never asking the same question twice.

## 6. How this package delivers it

- **Models first:** objects/tenancy (02), workflow/states (03), the five-lane responsibility map (04), the covenant-tracking model with the engine-to-surface contract grounded in the real evidence (05), lineage/provenance (06), ownership (07).
- **The frame:** shell plan with the rail proposal and palette grammar (08); responsive/co-visibility law (11).
- **18 surface briefs** (surface-briefs/) at the Basis-deep-dive bar — every one with the full 18-section template: anatomy, states, five-lane table, co-visibility matrix, viewports, benchmark take/adapt/reject with official citations, acceptance tests on the shared fixtures.
- **12 cross-cutting system specs** (cross-cutting/) — identity, period/seal, provenance (the lit-row contract that fixes U1-F1 once), document artifacts, certify/send safety, status vocabulary, tables, charts, notifications, search/keyboard, roles/tenancy, accessibility.
- **Research honestly done:** six dynamic sweeps over current official documentation (09 + benchmark-registry.yml) — 45+ products with limited roles, 15 discovered beyond the seeds, 6 anti-patterns; the synthesis is Covenant-specific, not "make it like X."
- **A roadmap built to survive the feasibility gate** (12): six foundations → five vertical slices on the send-vertical pattern → five surface waves, every item naming its basis-v2 landing place, with the fixture burndown measured (B9) and work orders (13) + fixtures/tests (14) binding "done" to proof.
- **Nine decisions for Terry** (16) — only the genuinely non-resolvable, each with a recommendation.

## 7. Risks and unresolved evidence (top five; full ledger in 01 §2 and 16)

1. **Repository verification is snapshot-mediated** — this session could not attach basis-v2 (cross-owner restriction); load-bearing claims are cited to snapshot-named addresses and flagged where unverified. The feasibility gate closes this by construction.
2. **Seven primary references never arrived in the frozen zip** (C-2) — attach before ratification (D-9).
3. **Formula correctness is expressly not certified** — the semantic-risk register (05 §7: debt-service basis, annualization, occupancy denominators, watch-band default, NOI boundary) is the separate audit's work list.
4. **Persistence ground truth** — two migrations' application unconfirmed; F2 begins by confirming, not assuming.
5. **The parity law** — Basis's power-user kernel/keyboard system and governance machinery must keep their Covenant equivalents specced here at parity-or-better as both products evolve; the cross-cutting keyboard/agent specs are the current answer, and parity should be re-checked at each wave exit.

**Bottom line:** nothing in the current build's architecture blocks the target; the plan's work is wiring, machinery, and discipline — not reinvention. The first three P0 orders (the sign-in link, the lit-row repair, the badge semantics) are shippable the day this plan is ratified.
